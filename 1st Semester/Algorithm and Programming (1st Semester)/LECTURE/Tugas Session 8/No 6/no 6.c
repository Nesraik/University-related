#include <stdio.h>
int main() {
	int x,y;
	for(x=1;x<=3;x++);
			for(y=3;y>=1;y--);
				printf("%d %d ",x,y);		
	return 0;
}
/* The output of the code above is: 
4 0 
*/
int main() {
	int x,y;
	for(x=1;x<=3;x++);
			for(y=3;y>=1;y--);
				printf("%d %d ",x,y);		
	return 0;
}
/* The output of the code above is
4 0
*/ 
