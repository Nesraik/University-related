#include <stdio.h>
#include <stdlib.h>
int main() {
	for(i=k;i < n; i++) printf("Binus\n");
	return 0;
}
/* since i=k, hence k<n
a) The statement will be executed and the number of times the statement is executed will depend
on the value of n and k (as long as k still meets the condition of k<n).
b) The statement will never be executed.
c) The statement will never be executed.
*/ 
